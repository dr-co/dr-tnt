language: perl
perl:
  - "5.14"
  - "5.16"
  - "5.18"
  - "5.20"
before_install:
  - echo "deb http://ftp.de.debian.org/debian sid main" | sudo tee -a /etc/apt/sources.list
  - sudo apt-get -qq update
  - sudo apt-get -qq install -t sid tarantool
